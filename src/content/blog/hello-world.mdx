---
title: "Hello World - My First MDX Blog Post"
date: "2025-04-04"
excerpt: "This is my first blog post using MDX, featuring code blocks, images, and more."
tags: ["test"]
author: "Daniel"
coverImage: "/images/large-image.jpg"
readingTime: "3 min read"
---

## Welcome to My MDX Blog

This is a test post for my MDX-powered blog. MDX combines the power of Markdown with JSX components.

## Code Blocks

Here's a simple code snippet:

```ts
// A simple greeting function
function greeting(name: string) {
  return `Hello, ${name}!`;
}

console.log(greeting("World"));
```

## TypeScript Editor with Type Information

One of the coolest features of this blog is the ability to show TypeScript code with type information. Hover over variables and functions to see their types:

<MonacoCodeBlock filename="types-example.ts" id="user-interface-example" language="typescript" height="400px">
{`// TypeScript interface definition
interface User {
  id: number;
  name: string;
  email: string;
  role: "admin" | "user" | "guest";
  age?: number;
}

// Function with type annotations
function getUserDisplayName(user: User): string {
return user.role === "admin"
? \`\${user.name} (Admin)\`
: user.name;
}

// Example usage
const currentUser: User = {
id: 1,
name: "Daniel",
email: "daniel@example.com",
role: "admin"
};

const displayName = getUserDisplayName(currentUser);
console.log(displayName); // Output: "Daniel (Admin)"
`}

</MonacoCodeBlock>

## Interactive TypeScript Editor

You can even make the editor interactive, allowing you to edit and experiment with the code:

<MonacoCodeBlock filename="interactive-example.tsx" editable={true} height="350px" id="react-example" language="typescript">
{`
import React, { useState } from 'react';

function Greeting() {
const [showMessage, setShowMessage] = useState(true);

return (

<div style={{ padding: "1rem", fontFamily: "Arial" }}>
  <h1>Welcome to React!</h1>
  {showMessage && <p>Hello there! 👋</p>}
  <button onClick={() => setShowMessage(!showMessage)}>
    {showMessage ? "Hide" : "Show"} Message
  </button>
</div>
); }

export default Greeting;
`}

</MonacoCodeBlock>

## Images

You can include images in your MDX posts:

![Sample Image](/images/placeholder.jpg)

## Custom Components

One of the best features of MDX is the ability to use React components within your content:

<Alert title="Important Information" variant="error">
  This is an alert component that can be reused throughout your MDX content.
</Alert>

<CalloutBox title="Pro Tip">
  You can create custom components to enhance your blog posts with interactive
  elements.
</CalloutBox>

## YouTube Videos

You can easily embed YouTube videos:

<YouTube id="dQw4w9WgXcQ" title="Never Gonna Give You Up" />

## Links

You can [link to external sites](https://nextjs.org) or to [other pages](/blog) within your site.

## Blockquotes

> "The best way to predict the future is to invent it."
> — Alan Kay

Happy blogging!
